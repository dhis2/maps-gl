import Layer from "./Layer";

class Dots extends Layer {
  constructor(config) {
    super();
  }

  createSource() {}

  createLayers() {}

  setOpacity(opacity) {
    if (this.isOnMap()) {
    }
  }
}

export default Dots;
