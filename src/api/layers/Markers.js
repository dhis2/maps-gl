import Layer from "./Layer";

class Markers extends Layer {
  constructor(config) {
    super();
  }

  createSource() {}

  createLayers() {}

  setOpacity(opacity) {
    if (this.isOnMap()) {
    }
  }
}

export default Markers;
